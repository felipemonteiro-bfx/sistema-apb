rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Acesso direto (sem login) - permite leitura e escrita
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
