# Workflow mínimo para diagnosticar Startup failure
# Se este também falhar, o problema é em Settings > Actions do repositório
name: Test Minimal

on:
  push:
    branches: [master]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: echo "OK"
